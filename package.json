{
    "name": "nuvio-emby-plugin",
    "version": "1.0.0",
    "description": "Plugin do Nuvio para reproduzir midia diretamente do meu Emby Server local",
    "scripts": {
        "build": "node build.js",
        "build:watch": "nodemon --watch src -e js,json --exec 'node build.js'"
    },
    "devDependencies": {
        "esbuild": "^0.20.0",
        "nodemon": "^3.0.0"
    },
    "license": "MIT"
}
